if ! command -v gitleaks >/dev/null 2>&1; then
  echo "ERROR: gitleaks is not installed. Run 'brew install gitleaks' (see README)." >&2
  exit 1
fi
gitleaks git --staged --redact --no-banner
bun run check
